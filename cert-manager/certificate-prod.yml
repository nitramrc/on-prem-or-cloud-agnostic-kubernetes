apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: myapp
  namespace: default
spec:
  secretName: myapp-tls-prod
  issuerRef:
    name: myapp-letsncrypt-prod
  commonName: myapp.overbinary.org
  #dnsNames:
  #- www.myapp.overbinary.org
  acme:
    config:
    - http01:
        ingress: myapp
      domains:
      - myapp.overbinary.org
      #- www.myapp.overbinary.org
